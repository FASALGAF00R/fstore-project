
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Forgot Password</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body style="background-color:lightgray;">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6" style="max-width: 505px; margin-top: 147px;">
        <div class="card mt-5">
          <div class="card-header">
            <h3>Forgot Password</h3>
          </div>
          <div class="card-body">
            <form action="/forgotpassword" method="POST" >
              <!-- Email Entry Form -->
              <div class="form-group" style="margin: 4px; width: 443px; height: 172px;">
                <label for="email">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" style="width: 278px;" >
                
              </div>
              <button type="submit" class="btn btn-primary" id="sendOTPButton">Send OTP</button>
            </form>
            <% if (showOTPForm) { %>
            <!-- OTP Entry Form -->
            <form action="/verifyforgototp" method="post" <% if (!showOTPForm) { %>style="display: none;"<% } %> >

  <div class="form-group" style="margin: 4px; width: 443px; height: 172px;">
    <label for="otp">OTP</label>
    <input type="text" class="form-control" id="otp" name="otp" placeholder="Enter the OTP" style="width: 278px;" required>
  </div>
  <button type="submit" class="btn btn-primary">Verify</button>
  <% } %>
  <% if (message !== undefined && message !== '') { %>
    <p style="color: #fb4e37;">
      <%= message %>
    </p>
  <% } %>
  
</form>
</div>
  </div>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    $(document).ready(function() {
      // Disable the Send OTP button initially
      $('#sendOTPButton').prop('disabled', true);
    
      // Listen for changes in the email input field
      $('#email').on('input', function() {
        // Check if the email input is not empty
        if ($(this).val().trim() !== '') {
          // If not empty, enable the Send OTP button
          $('#sendOTPButton').prop('disabled', false);
        } else {
          // If empty, disable the Send OTP button
          $('#sendOTPButton').prop('disabled', true);
        }
      });
    });
    </script>
    
</body>
</html>
